<tr>
  <td class="six sub-columns">
    <strong>
      <%= link_to raw(line_item.variant.product.name), product_url(line_item.variant.product) %>
    </strong>
    <%= raw(line_item.variant.options_text) -%>
    (<%= line_item.variant.sku %>)
  </td>
  <td class="six sub-columns last right">
    <%= line_item.quantity %> x <%= line_item.single_money.to_money.to_display %> = <%= line_item.display_amount.to_display %>
  </td>
</tr>
