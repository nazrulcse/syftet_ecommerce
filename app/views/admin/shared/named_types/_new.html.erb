<% content_for :page_title do %>
  <%= link_to collection_url_label, collection_url %> /
  <%= page_title %>
<% end %>

<%= render partial: 'admin/shared/error_messages', locals: { target: @object } %>

<%= form_for [:admin, @object] do |f| %>
  <fieldset>
    <%= render partial: 'admin/shared/named_types/form', locals: { f: f } %>
    <%= render partial: 'admin/shared/new_resource_links' %>
  </fieldset>
<% end %>
