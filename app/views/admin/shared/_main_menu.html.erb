<% if can? :admin, Order %>
    <ul class="nav nav-sidebar">
      <%= tab *BackendConfiguration::ORDER_TABS, icon: 'shopping-cart' %>
    </ul>
<% end %>

<%# if can?(:admin, ReturnAuthorization) || can?(:admin, CustomerReturn) %>
<ul class="nav nav-sidebar">
  <%= main_menu_tree t(:returns), icon: "transfer", sub_menu: "returns", url: "#sidebar-returns" %>
</ul>
<%# end %>

<% if can? :admin, Product %>
    <ul class="nav nav-sidebar">
      <%= main_menu_tree t(:products), icon: "th-large", sub_menu: "product", url: "#sidebar-product" %>
    </ul>
<% end %>

<% if can? :admin, Admin::ReportsController %>
    <ul class="nav nav-sidebar">
      <%= tab *BackendConfiguration::REPORT_TABS, icon: 'file' %>
    </ul>
<% end %>

<% if can? :admin, Promotion %>
    <ul class="nav nav-sidebar">
      <%= main_menu_tree t(:promotions), icon: "gift", sub_menu: "promotion", url: "#sidebar-promotions" %>
    </ul>
<% end %>

<% if can?(:admin, User) %>
    <ul class="nav nav-sidebar">
      <%= tab *BackendConfiguration::USER_TABS, url: admin_users_path, icon: 'user' %>
    </ul>
<% end %>

<% if can?(:admin, Blog) %>
    <ul class="nav nav-sidebar">
      <%= tab *BackendConfiguration::BLOG_TABS, url: admin_blogs_path, icon: 'pencil' %>
    </ul>
<% end %>

<!--current_store-->
<% if can? :admin, nil %>
    <ul class="nav nav-sidebar">
      <%= main_menu_tree t(:configurations), icon: "wrench", sub_menu: "configuration", url: "#sidebar-configuration" %>
    </ul>
<% end %>
