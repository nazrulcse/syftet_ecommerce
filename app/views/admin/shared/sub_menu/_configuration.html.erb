<ul data-hook="admin_configurations_sidebar_menu" class="collapse nav nav-pills nav-stacked" id="sidebar-configuration">
  <%#= configurations_sidebar_menu_item(t(:general_settings), edit_admin_general_settings_path) if can? :manage, Config %>
  <%#= configurations_sidebar_menu_item(t(:tax_categories), admin_tax_categories_path) if can? :manage, TaxCategory %>
  <%#= configurations_sidebar_menu_item(t(:tax_rates), admin_tax_rates_path) if can? :manage, TaxRate %>
  <%#= configurations_sidebar_menu_item(t(:zones), admin_zones_path) if can? :manage, Zone %>
  <%#= configurations_sidebar_menu_item(t(:countries), admin_countries_path) if can? :manage, Country %>

  <%# if country = Country.find_by_id(Config[:default_country_id]) || Country.first %>
  <%#= configurations_sidebar_menu_item(t(:states), admin_country_states_path(country)) if can? :manage, Country %>
  <%# end %>

  <%= configurations_sidebar_menu_item(t(:payment_methods), admin_payment_methods_path) if can? :manage, PaymentMethod %>
  <%= configurations_sidebar_menu_item(t(:shipping_methods), admin_shipping_methods_path) if can? :manage, ShippingMethod %>
  <%= configurations_sidebar_menu_item(t(:shipping_categories), admin_shipping_categories_path) if can? :manage, ShippingCategory %>
  <%= configurations_sidebar_menu_item(t(:stock_transfers), admin_stock_transfers_path) if can? :manage, StockTransfer %>
  <%= configurations_sidebar_menu_item(t(:stock_locations), admin_stock_locations_path) if can? :manage, StockLocation %>
  <%#= configurations_sidebar_menu_item(t(:store_credit_categories), admin_store_credit_categories_path) if can? :manage, StoreCreditCategory %>
  <%#= configurations_sidebar_menu_item(t(:analytics_trackers), admin_trackers_path) if can? :manage, Tracker %>
  <%= configurations_sidebar_menu_item(t(:refund_reasons), admin_refund_reasons_path) if can? :manage, RefundReason %>
  <%#= configurations_sidebar_menu_item(t(:reimbursement_types), admin_reimbursement_types_path) if can? :manage, ReimbursementType %>
  <%#= configurations_sidebar_menu_item(t(:return_authorization_reasons), admin_return_authorization_reasons_path) if can? :manage, ReturnAuthorizationReason %>
  <%= configurations_sidebar_menu_item(t(:roles), admin_roles_path) if can? :manage, Role %>
</ul>
