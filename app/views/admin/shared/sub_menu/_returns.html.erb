<ul id="sidebar-returns" class="collapse nav nav-pills nav-stacked" data-hook="admin_returns_sub_tabs">
  <%= tab :return_authorizations, url: admin_return_authorizations_path, match_path: '/return_authorizations' if can?(:admin, ReturnAuthorization) %>
  <%= tab :customer_returns, url: admin_customer_returns_path, match_path: '/customer_returns' if can?(:admin, CustomerReturn) %>
</ul>
